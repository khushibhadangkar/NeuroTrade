# Copy this file to .env.local and fill in your values.

# Flask backend URL (server-side only — used in next.config.ts rewrites)
NEUROTRADE_API_URL=http://localhost:5000

# Set to true to enable debug logging in the Next.js app
NEXT_PUBLIC_DEBUG=false
